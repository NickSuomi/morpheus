# Agent Instructions

Always read project guidance before working:

- `docs/product/PRD.md`
- `CONTEXT.md`
- `ARCHITECTURE.md`
- relevant files in `docs/adr/`
- `docs/agents/*.md`

Use Beads for task tracking. Do not create markdown task lists as tracker substitutes.

Keep public product vocabulary as Morpheus. Use Sandcastle only for the low-level adapter around `@ai-hero/sandcastle`.

## Commits

Use `.gitmessage` as the commit template.

Commit messages should include:

- why this change exists
- what changed
- how it was verified
- Beads issue refs when applicable

## Agent skills

### Issue tracker

Issues are tracked locally with Beads (`bd`) using prefix `morph`. See `docs/agents/issue-tracker.md`.

### Triage labels

Matt Pocock triage labels use defaults; Morpheus `agent:*` workflow labels are separate. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` plus root `docs/adr/`. See `docs/agents/domain.md`.

<!-- BEGIN BEADS CODEX SETUP: generated by bd setup codex -->
## Beads Issue Tracker

Use Beads (`bd`) for durable task tracking in repositories that include it. Use the `beads` skill at `.agents/skills/beads/SKILL.md` (project install) or `~/.agents/skills/beads/SKILL.md` (global install) for Beads workflow guidance, then use the `bd` CLI for issue operations.

### Quick Reference

```bash
bd ready                # Find available work
bd show <id>            # View issue details
bd update <id> --claim  # Claim work
bd close <id>           # Complete work
bd prime                # Refresh Beads context
```

### Rules

- Use `bd` for all task tracking; do not create markdown TODO lists.
- Run `bd prime` when Beads context is missing or stale.
- Keep persistent project memory in Beads via `bd remember`; do not create ad hoc memory files.
<!-- END BEADS CODEX SETUP -->
